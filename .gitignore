node_modules
# Keep environment variables out of version control
.env

/generated/prisma
/dist

# Customer display uploads (keep folder via .gitkeep)
uploads/customer-display/*
!uploads/customer-display/.gitkeep

# Product uploads (keep folder via .gitkeep)
uploads/products/*
!uploads/products/.gitkeep